#! /bin/bash
#
# Get information about DSpace items of any type.
#
# Usage: dspace_item [DEPLOYMENT] [OPTIONS] HANDLE...
# Usage: dspace_item --help

PROGRAM=`realpath "$0"`
BIN_DIR=`dirname "$PROGRAM"`
PROGNAME=`basename "$PROGRAM"`

exec "$BIN_DIR/run_ruby" "$PROGNAME" "$@"
