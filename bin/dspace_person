#! /bin/bash
#
# Get information about DSpace Person entities.
#
# Usage: dspace_person [DEPLOYMENT] [OPTIONS] [TERM...]
# Usage: dspace_person --help
#
# Each TERM argument may be a computing ID, a last name, a first and last name
# surrounded by quotes, or a "last, first" name in bibliographic order.
# With no arguments, all Persons are listed.

PROGRAM=`realpath "$0"`
BIN_DIR=`dirname "$PROGRAM"`
PROGNAME=`basename "$PROGRAM"`

source "$BIN_DIR/dspace_values"

exec "$BIN_DIR/run_ruby" "$PROGNAME" "$@"
