#! /bin/bash
#
# Get information about DSpace Person entities.
#
# Usage: dspace_person [DEPLOYMENT] [OPTIONS] [NAME...]
# Usage: dspace_person --help
#
# Each `name` argument may be a computing ID, a last name, a first and last
# name surrounded by quotes, or a "last, first" name in bibliographic order.

PROGRAM=`realpath "$0"`
BIN_DIR=`dirname "$PROGRAM"`
PROGNAME=`basename "$PROGRAM"`

exec "$BIN_DIR/run_ruby" "$PROGNAME" "$@"
