#! /bin/bash
#
# Script values -- intended to be "sourced" not run -- within a DSpace shell.

ERROR_STATUS=127

# =============================================================================
# Functions
# =============================================================================

function abort() # output_lines...
{
    for MSG in "$@" ; do
        echo "$MSG" 1>&2
    done
    exit $ERROR_STATUS
}
